# 使用示例

本文档提供项目各个功能的使用示例。

---

## 一、本地测试

### 1.1 启动测试脚本

```bash
# 方式1: 直接运行
python test_api.py

# 方式2: 使用启动脚本
./run.sh
# 选择选项 3 (测试模式)
```

### 1.2 测试简道云API

```python
# 在test_api.py中或Python交互式环境
from utils.jiandaoyun_api import JiandaoyunAPI

jdy = JiandaoyunAPI()

# 查询员工信息
employee = jdy.query_employee_by_phone('13800138000')
print(employee)

# 获取所有已绑定员工
employees = jdy.get_all_employees_with_openid()
print(f'找到 {len(employees)} 个已绑定员工')

# 查询今日日报
from datetime import datetime
today = datetime.now().strftime('%Y-%m-%d')
report = jdy.get_daily_report('13800138000', today)
print(report)
```

### 1.3 测试微信API

```python
from utils.wechat_api import WeChatAPI

wechat = WeChatAPI()

# 获取access_token
token = wechat.get_access_token()
print(f'Token: {token}')

# 发送测试消息（需要真实OpenID）
success = wechat.send_daily_report_reminder(
    openid='test_openid',
    employee_name='张三',
    report_status='pending',
    report_url='https://www.jiandaoyun.com'
)
print(f'发送结果: {success}')
```

---

## 二、启动应用

### 2.1 开发模式

```bash
# 方式1: 直接运行
python app.py

# 方式2: 使用启动脚本
./run.sh
# 选择选项 1 (开发模式)
```

输出示例：
```
定时任务已启动，每天18:00执行日报检查
 * Serving Flask app 'app'
 * Debug mode: on
 * Running on http://0.0.0.0:5000
```

### 2.2 生产模式

```bash
# 方式1: 使用gunicorn
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app

# 方式2: 使用启动脚本
./run.sh
# 选择选项 2 (生产模式)

# 方式3: 后台运行
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > gunicorn.log 2>&1 &
```

---

## 三、API接口使用

### 3.1 检查系统状态

```bash
curl http://localhost:5000/
```

响应示例：
```json
{
  "status": "ok",
  "message": "简道云日报提醒系统运行中"
}
```

### 3.2 员工身份绑定

```bash
curl -X POST http://localhost:5000/api/bind \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "13800138000",
    "openid": "test_openid_123"
  }'
```

成功响应：
```json
{
  "success": true,
  "message": "绑定成功！",
  "data": {
    "name": "张三",
    "phone": "13800138000"
  }
}
```

失败响应（未找到员工）：
```json
{
  "success": false,
  "message": "未找到该手机号对应的员工档案，请联系组织管理员添加员工档案"
}
```

### 3.3 检查绑定状态

```bash
curl "http://localhost:5000/api/check_bind?openid=test_openid_123"
```

响应示例：
```json
{
  "success": true,
  "bound": false
}
```

### 3.4 手动触发日报检查

```bash
curl -X POST http://localhost:5000/api/test_reminder
```

响应示例：
```json
{
  "success": true,
  "message": "日报检查任务已执行，请查看控制台日志"
}
```

---

## 四、定时任务使用

### 4.1 自动执行

应用启动后，定时任务会自动启动，每天18:00执行。

控制台日志示例：
```
[2026-01-23 18:00:00] 开始执行日报检查任务...
找到 15 个已绑定成员
管理员数量: 3
检查成员: 张三 (员工) - 13800138000
  ✓ 张三 已填写日报
  → 已通知员工本人
  → 已通知管理员: 李经理
  → 已通知管理员: 王总监
检查成员: 李四 (员工) - 13900139000
  ✗ 李四 未填写日报
  → 已发送提醒
[2026-01-23 18:00:15] 日报检查任务完成
```

### 4.2 手动触发

```python
from scheduler import DailyReportScheduler

scheduler = DailyReportScheduler()
scheduler.run_now()  # 立即执行一次
```

或使用API接口：
```bash
curl -X POST http://localhost:5000/api/test_reminder
```

### 4.3 修改执行时间

编辑 `scheduler.py` 文件：

```python
# 修改这一行
self.scheduler.add_job(
    self.check_and_send_reminders,
    'cron',
    hour=18,  # 修改为你想要的小时（0-23）
    minute=0,  # 修改为你想要的分钟（0-59）
    id='daily_report_reminder'
)
```

---

## 五、微信服务号配置（域名通过后）

### 5.1 配置服务器地址

1. 登录微信公众平台：https://mp.weixin.qq.com
2. 进入"开发" -> "基本配置"
3. 填写配置：
   - URL: `http://your-domain.com/wechat`
   - Token: `MyWeChat2026htjiac`（与app.py中的mytoken一致）
   - EncodingAESKey: 随机生成
   - 消息加解密方式: 明文模式
4. 点击"提交"

### 5.2 验证服务器

微信会发送GET请求验证服务器：

```
GET /wechat?signature=xxx&timestamp=xxx&nonce=xxx&echostr=xxx
```

应用会自动验证并返回echostr。

### 5.3 配置自定义菜单

在微信公众平台：
1. 进入"自定义菜单"
2. 添加菜单按钮：
   - 菜单名称: "身份绑定"
   - 菜单类型: 跳转网页
   - 页面地址: `http://your-domain.com/bind?openid=OPENID`

注意：`OPENID` 是微信的占位符，会自动替换。

### 5.4 用户绑定流程

1. 用户关注服务号
2. 点击"身份绑定"菜单
3. 进入绑定页面（自动获取OpenID）
4. 输入手机号
5. 提交绑定
6. 显示绑定结果

---

## 六、消息推送示例

### 6.1 员工已填写日报

**发送给员工本人：**
```
标题: 日报提交成功
内容:
  员工姓名: 张三
  提交时间: 2026-01-23 17:30:00
  备注: 点击查看日报详情
```

**发送给管理员：**
```
标题: 员工日报提交通知
内容:
  员工姓名: 张三
  提交时间: 2026-01-23 17:30:00
  备注: 点击查看该员工日报详情
```

### 6.2 员工未填写日报

**发送给员工本人：**
```
标题: 日报填写提醒
内容:
  员工姓名: 张三
  提醒时间: 2026-01-23 18:00:00
  备注: 请及时填写日报，点击进入填写页面
```

---

## 七、常见使用场景

### 场景1: 新员工入职

1. 在简道云员工档案中添加新员工信息（包含手机号）
2. 通知员工关注微信服务号
3. 员工点击"身份绑定"菜单
4. 输入手机号完成绑定
5. 从当天开始接收日报提醒

### 场景2: 员工离职

1. 在简道云员工档案中删除或停用员工
2. 系统自动不再向该员工发送提醒

### 场景3: 管理员查看日报

1. 管理员每天18:00后收到员工日报提交通知
2. 点击消息查看员工日报详情
3. 了解团队工作进展

### 场景4: 测试功能

```bash
# 1. 启动应用
python app.py

# 2. 在另一个终端手动触发
curl -X POST http://localhost:5000/api/test_reminder

# 3. 查看控制台日志
# 观察执行过程和结果
```

### 场景5: 修改提醒时间

```python
# 编辑 scheduler.py
# 将18:00改为17:00
self.scheduler.add_job(
    self.check_and_send_reminders,
    'cron',
    hour=17,  # 改为17
    minute=0,
    id='daily_report_reminder'
)

# 重启应用
```

---

## 八、调试技巧

### 8.1 查看详细日志

应用运行时会输出详细日志，包括：
- API调用结果
- 员工检查过程
- 消息发送状态
- 错误信息

### 8.2 测试单个功能

```python
# 测试查询员工
from utils.jiandaoyun_api import JiandaoyunAPI
jdy = JiandaoyunAPI()
employee = jdy.query_employee_by_phone('13800138000')
print(employee)

# 测试获取token
from utils.wechat_api import WeChatAPI
wechat = WeChatAPI()
token = wechat.get_access_token()
print(token)
```

### 8.3 模拟不同场景

在简道云中：
1. 添加/删除员工的wxopenid字段（模拟绑定/未绑定）
2. 添加/删除今日日报记录（模拟已填写/未填写）
3. 修改员工角色（模拟员工/管理员）

### 8.4 检查API响应

```bash
# 使用curl -v查看详细响应
curl -v http://localhost:5000/

# 使用jq格式化JSON响应
curl http://localhost:5000/ | jq
```

---

## 九、故障排查

### 问题1: 应用启动失败

```bash
# 检查依赖
pip list | grep -E "Flask|requests|APScheduler"

# 重新安装依赖
pip install -r requirements.txt

# 检查Python版本
python --version  # 需要3.8+
```

### 问题2: API调用失败

```python
# 检查配置
from config import Config
print(Config.JIANDAOYUN_API_KEY)
print(Config.WECHAT_APPID)

# 测试API连接
import requests
response = requests.get('https://api.jiandaoyun.com')
print(response.status_code)
```

### 问题3: 定时任务未执行

```python
# 检查任务是否添加
from scheduler import DailyReportScheduler
scheduler = DailyReportScheduler()
scheduler.start()
print(scheduler.scheduler.get_jobs())

# 手动触发测试
scheduler.run_now()
```

### 问题4: 消息发送失败

```python
# 检查access_token
from utils.wechat_api import WeChatAPI
wechat = WeChatAPI()
token = wechat.get_access_token()
print(f'Token: {token}')

# 检查OpenID是否有效
# OpenID必须是真实用户的，测试OpenID无法发送消息
```

---

## 十、最佳实践

### 1. 开发环境
- 使用虚拟环境隔离依赖
- 使用测试脚本验证功能
- 查看详细日志排查问题

### 2. 生产环境
- 使用gunicorn运行应用
- 配置systemd管理服务
- 使用Nginx反向代理
- 配置日志轮转
- 定期备份数据

### 3. 安全建议
- 修改SECRET_KEY
- 使用HTTPS协议
- 限制API访问频率
- 不要暴露敏感信息

### 4. 性能优化
- 使用缓存减少API调用
- 优化数据库查询
- 监控系统资源使用

---

**文档版本**: v1.0  
**更新日期**: 2026-01-23
