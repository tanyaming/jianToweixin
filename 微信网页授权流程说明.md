# 微信网页授权流程说明

## 📋 业务逻辑概述

微信服务号网页授权是获取用户OpenID的标准流程，用于在网页中识别用户身份。

---

## 🔄 完整授权流程

### 1. 用户触发授权
```
用户在微信中点击菜单 "身份绑定"
    ↓
跳转到: https://htjc.shop/get_openid
```

### 2. 第一次访问（无code）
```python
# app.py - /get_openid 路由
@app.route('/get_openid', methods=['GET'])
def get_openid():
    code = request.args.get('code', '')
    
    if not code:
        # 构造微信授权URL
        redirect_uri = f"{Config.BASE_URL}/get_openid"
        auth_url = wechat_api.get_oauth_authorize_url(
            redirect_uri=redirect_uri,
            scope='snsapi_base',  # 静默授权
            state='bind'
        )
        # 重定向到微信授权页面
        return redirect(auth_url)
```

**生成的授权URL格式**：
```
https://open.weixin.qq.com/connect/oauth2/authorize?
  appid=wx4fce2b77e1ad0638
  &redirect_uri=https%3A%2F%2Fhtjc.shop%2Fget_openid
  &response_type=code
  &scope=snsapi_base
  &state=bind
  #wechat_redirect
```

### 3. 微信授权页面
```
用户在微信内自动完成授权（snsapi_base为静默授权）
    ↓
微信重定向回: https://htjc.shop/get_openid?code=CODE&state=bind
```

### 4. 第二次访问（有code）
```python
# app.py - /get_openid 路由
if code:
    # 使用code换取openid
    oauth_data = wechat_api.get_oauth_access_token(code)
    
    if oauth_data and 'openid' in oauth_data:
        openid = oauth_data['openid']
        # 重定向到绑定页面
        return redirect(f"/bind?openid={openid}")
```

**微信API调用**：
```
GET https://api.weixin.qq.com/sns/oauth2/access_token?
  appid=wx4fce2b77e1ad0638
  &secret=8f1d12b6e8379e4aabe5b00fe95e7cd2
  &code=CODE
  &grant_type=authorization_code
```

**返回数据**：
```json
{
  "access_token": "ACCESS_TOKEN",
  "expires_in": 7200,
  "refresh_token": "REFRESH_TOKEN",
  "openid": "OPENID",
  "scope": "snsapi_base"
}
```

### 5. 跳转到绑定页面
```
https://htjc.shop/bind?openid=OPENID
    ↓
用户输入手机号完成绑定
```

---

## 🔑 两种授权作用域

### snsapi_base（静默授权）
- **特点**：用户无感知，自动完成授权
- **获取信息**：只能获取openid
- **使用场景**：只需要识别用户身份
- **本项目使用**：✅ 推荐使用

### snsapi_userinfo（用户授权）
- **特点**：需要用户手动点击确认授权
- **获取信息**：可获取用户昵称、头像、性别等
- **使用场景**：需要展示用户信息
- **本项目使用**：❌ 不需要

---

## 📦 wechat_api.py 核心方法

### 1. 生成授权URL
```python
def get_oauth_authorize_url(self, redirect_uri: str, 
                           scope: str = 'snsapi_base', 
                           state: str = 'STATE') -> str:
    """
    生成微信网页授权URL
    
    Args:
        redirect_uri: 授权后重定向的回调链接（需URL编码）
        scope: snsapi_base 或 snsapi_userinfo
        state: 自定义参数，防止CSRF攻击
    
    Returns:
        完整的微信授权URL
    """
```

### 2. 换取access_token和openid
```python
def get_oauth_access_token(self, code: str) -> Optional[Dict]:
    """
    通过code换取网页授权access_token和openid
    
    Args:
        code: 微信授权回调返回的code参数（5分钟有效，只能使用一次）
    
    Returns:
        包含access_token、openid等信息的字典
    """
```

### 3. 获取用户信息（可选）
```python
def get_oauth_userinfo(self, oauth_access_token: str, 
                      openid: str) -> Optional[Dict]:
    """
    拉取用户信息（需scope为snsapi_userinfo）
    
    Args:
        oauth_access_token: 网页授权access_token
        openid: 用户openid
    
    Returns:
        用户详细信息
    """
```

---

## ⚠️ 重要注意事项

### 1. 两种access_token的区别

| 类型 | 获取方式 | 用途 | 有效期 |
|------|---------|------|--------|
| **基础支持access_token** | `get_access_token()` | 主动调用API（发送模板消息等） | 7200秒 |
| **网页授权access_token** | `get_oauth_access_token(code)` | 获取用户信息 | 7200秒 |

**不能混用！**

### 2. code的特性
- **有效期**：5分钟
- **使用次数**：只能使用一次
- **获取方式**：通过微信授权回调获得

### 3. redirect_uri要求
- 必须与微信公众平台配置的**网页授权域名**一致
- 需要进行URL编码
- 必须使用HTTPS（认证后的服务号）

### 4. 域名配置
在微信公众平台配置：
```
设置与开发 → 公众号设置 → 功能设置 → 网页授权域名
添加: htjc.shop
```

---

## 🧪 测试流程

### 1. 本地测试（使用内网穿透）
```bash
# 使用ngrok或其他内网穿透工具
ngrok http 5000

# 获得临时域名，如: https://abc123.ngrok.io
# 在微信公众平台配置此域名
```

### 2. 生产环境测试
```bash
# 1. 确保域名已配置SSL证书
curl -I https://htjc.shop

# 2. 确保服务正常运行
curl https://htjc.shop/

# 3. 在微信中访问
# 关注服务号 → 点击"身份绑定"菜单 → 查看是否正常跳转
```

### 3. 调试技巧
```python
# 在app.py中添加日志
app.logger.info(f'访问 /get_openid - code: {code}')
app.logger.info(f'微信API响应: {oauth_data}')
app.logger.info(f'获取到OpenID: {openid}')

# 查看日志
sudo journalctl -u jiandaoyun-daily -f
```

---

## 🔧 完整代码示例

### app.py
```python
@app.route('/get_openid', methods=['GET'])
def get_openid():
    """微信网页授权获取OpenID"""
    code = request.args.get('code', '')
    
    # 第一步：没有code，跳转到微信授权
    if not code:
        redirect_uri = f"{Config.BASE_URL}/get_openid"
        auth_url = wechat_api.get_oauth_authorize_url(
            redirect_uri=redirect_uri,
            scope='snsapi_base',
            state='bind'
        )
        return redirect(auth_url)
    
    # 第二步：有code，换取openid
    try:
        oauth_data = wechat_api.get_oauth_access_token(code)
        
        if oauth_data and 'openid' in oauth_data:
            openid = oauth_data['openid']
            # 跳转到绑定页面
            return redirect(f"/bind?openid={openid}")
        else:
            return "获取OpenID失败", 500
            
    except Exception as e:
        app.logger.error(f'授权异常: {e}')
        return "系统错误", 500
```

### wechat_api.py
```python
class WeChatAPI:
    def get_oauth_authorize_url(self, redirect_uri: str, 
                               scope: str = 'snsapi_base',
                               state: str = 'STATE') -> str:
        """生成微信授权URL"""
        encoded_uri = urllib.parse.quote(redirect_uri, safe='')
        return (
            f"https://open.weixin.qq.com/connect/oauth2/authorize?"
            f"appid={self.appid}"
            f"&redirect_uri={encoded_uri}"
            f"&response_type=code"
            f"&scope={scope}"
            f"&state={state}"
            f"#wechat_redirect"
        )
    
    def get_oauth_access_token(self, code: str) -> Optional[Dict]:
        """通过code换取openid"""
        url = 'https://api.weixin.qq.com/sns/oauth2/access_token'
        params = {
            'appid': self.appid,
            'secret': self.appsecret,
            'code': code,
            'grant_type': 'authorization_code'
        }
        
        response = requests.get(url, params=params, timeout=10)
        data = response.json()
        
        if 'openid' in data:
            return data
        return None
```

---

## ✅ 检查清单

- [ ] 微信公众平台配置网页授权域名：`htjc.shop`
- [ ] config.py中配置正确的APPID和APPSECRET
- [ ] config.py中配置BASE_URL：`https://htjc.shop`
- [ ] wechat_api.py实现网页授权方法
- [ ] app.py实现/get_openid路由
- [ ] 域名配置SSL证书（HTTPS）
- [ ] 服务正常运行并可访问
- [ ] 微信菜单配置跳转URL：`https://htjc.shop/get_openid`
- [ ] 测试完整授权流程

---

## 🎯 总结

**正确的业务逻辑**：
1. 用户点击菜单 → 访问 `/get_openid`
2. 系统生成授权URL → 重定向到微信授权页面
3. 用户授权（静默） → 微信回调 `/get_openid?code=XXX`
4. 系统用code换取openid → 重定向到 `/bind?openid=XXX`
5. 用户输入手机号 → 完成绑定

**关键点**：
- 使用 `snsapi_base` 静默授权
- code只能使用一次，5分钟有效
- 必须配置网页授权域名
- 生产环境必须使用HTTPS