# 快速开始指南

## 当前开发阶段

由于域名还未申请通过，暂时跳过微信服务号配置，先完成核心功能的开发和测试。

---

## 一、环境准备

### 1.1 安装依赖

```bash
# 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

### 1.2 检查配置

确认 `config.py` 中的配置信息正确：
- 简道云 APP_ID 和 API_KEY
- 微信服务号 APPID 和 APPSECRET
- 表单ID（员工档案表、日报数据表）

---

## 二、功能测试

### 2.1 运行测试脚本

```bash
python test_api.py
```

测试脚本提供交互式菜单：

**选项1：测试简道云API**
- 查询员工信息（通过手机号）
- 获取所有已绑定OpenID的员工
- 查询今日日报

**选项2：测试微信API**
- 获取access_token
- 发送测试消息（需要有效的OpenID）

**选项3：测试定时任务**
- 立即执行一次日报检查
- 查看完整的执行流程和日志

### 2.2 启动应用

```bash
python app.py
```

应用启动后：
- Flask服务运行在 `http://0.0.0.0:5000`
- 定时任务自动启动，每天18:00执行
- 控制台会显示详细日志

### 2.3 手动触发日报检查

在应用运行时，可以通过API手动触发：

```bash
# 在另一个终端执行
curl -X POST http://localhost:5000/api/test_reminder
```

---

## 三、核心功能说明

### 3.1 员工身份绑定

**接口**: `POST /api/bind`

**请求参数**:
```json
{
  "phone": "手机号",
  "openid": "微信OpenID"
}
```

**功能**:
1. 通过手机号查询简道云员工档案
2. 找到员工后，更新其wxopenid字段
3. 返回绑定结果

### 3.2 日报提醒（定时任务）

**执行时间**: 每天18:00

**执行流程**:
1. 获取所有已绑定OpenID的成员
2. 筛选出管理员列表
3. 遍历每个成员，查询今日日报
4. 根据日报状态发送不同消息：
   - **已填写**: 通知员工本人 + 通知所有管理员
   - **未填写**: 仅通知员工本人

**消息类型**:
- 员工本人（已填写）：您今日的工作日报已提交成功
- 管理员（员工已填写）：员工【姓名】已提交今日工作日报
- 员工本人（未填写）：提醒您填写今日工作日报

---

## 四、开发调试技巧

### 4.1 查看详细日志

应用运行时会输出详细日志：

```
[2026-01-23 18:00:00] 开始执行日报检查任务...
找到 15 个已绑定成员
管理员数量: 3
检查成员: 张三 (员工) - 13800138000
  ✓ 张三 已填写日报
  → 已通知员工本人
  → 已通知管理员: 李经理
检查成员: 李四 (员工) - 13900139000
  ✗ 李四 未填写日报
  → 已发送提醒
[2026-01-23 18:00:15] 日报检查任务完成
```

### 4.2 修改定时任务时间

如需修改执行时间，编辑 `scheduler.py`:

```python
# 修改这一行
self.scheduler.add_job(
    self.check_and_send_reminders,
    'cron',
    hour=18,  # 修改小时
    minute=0,  # 修改分钟
    id='daily_report_reminder'
)
```

### 4.3 测试不同场景

可以通过修改简道云数据来测试不同场景：
1. 添加/删除员工的wxopenid字段
2. 添加/删除今日日报记录
3. 修改员工的角色（员工/管理员）

---

## 五、常见问题

### Q1: 测试时提示"未找到已绑定OpenID的成员"

**原因**: 简道云员工档案中没有填写wxopenid字段

**解决**:
1. 手动在简道云员工档案中添加测试OpenID
2. 或通过绑定接口添加：
```bash
curl -X POST http://localhost:5000/api/bind \
  -H "Content-Type: application/json" \
  -d '{"phone": "13800138000", "openid": "test_openid_123"}'
```

### Q2: 微信消息发送失败

**可能原因**:
1. access_token获取失败（检查APPID和APPSECRET）
2. OpenID无效（需要真实的用户OpenID）
3. 模板消息ID不正确

**调试方法**:
- 运行 `python test_api.py` 选择"测试微信API"
- 查看access_token是否能正常获取
- 检查控制台的错误信息

### Q3: 定时任务没有执行

**检查**:
1. 确认应用正常启动（看到"定时任务已启动"提示）
2. 检查系统时间是否正确
3. 使用测试接口手动触发：`POST /api/test_reminder`

---

## 六、下一步计划

### 6.1 域名申请通过后

1. 配置微信服务号服务器地址
2. 设置自定义菜单（身份绑定）
3. 配置网页授权域名
4. 测试完整的用户绑定流程

### 6.2 功能优化

- [ ] 添加日报统计功能
- [ ] 支持自定义提醒时间
- [ ] 增加管理员控制面板
- [ ] 优化消息模板内容
- [ ] 添加错误重试机制

---

## 七、项目文件说明

```
.
├── app.py                  # Flask主应用
├── config.py              # 配置文件
├── scheduler.py           # 定时任务调度器
├── test_api.py           # API测试脚本
├── requirements.txt      # Python依赖
├── run.sh               # 启动脚本
├── utils/
│   ├── jiandaoyun_api.py  # 简道云API封装
│   └── wechat_api.py      # 微信API封装
├── templates/
│   └── bind.html          # 绑定页面
├── 产品需求文档.md
├── 部署指南.md
└── 快速开始.md
```

---

**开始测试**: `python test_api.py`  
**启动应用**: `python app.py`  
**手动触发**: `curl -X POST http://localhost:5000/api/test_reminder`
