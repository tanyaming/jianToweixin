# 🚀 快速部署指南（ICP备案通过后）

## 第一步：上传代码到服务器

### 方法1：使用SCP（最简单）

在你的本地电脑上执行：

```bash
# 打包项目（排除不需要的文件）
cd 你的项目目录
tar -czf jiandaoyun-daily.tar.gz \
  --exclude='venv' \
  --exclude='__pycache__' \
  --exclude='.git' \
  --exclude='*.pyc' \
  .

# 上传到服务器
scp jiandaoyun-daily.tar.gz root@8.137.123.138:/opt/

# 连接到服务器
ssh root@8.137.123.168

# 解压
cd /opt
tar -xzf jiandaoyun-daily.tar.gz -C jiandaoyun-daily
cd jiandaoyun-daily
```

### 方法2：使用Git

```bash
# 在服务器上执行
ssh root@8.137.123.136
cd /opt
git clone <你的仓库地址> jiandaoyun-daily
cd jiandaoyun-daily
```

---

## 第二步：安装依赖

```bash
# 安装Python和必要工具
yum install python3 python3-pip -y  # CentOS
# 或
apt install python3 python3-pip -y  # Ubuntu

# 创建虚拟环境
cd /opt/jiandaoyun-daily
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
pip install gunicorn
```

---

## 第三步：配置防火墙

```bash
# 开放5000端口
firewall-cmd --zone=public --add-port=5000/tcp --permanent
firewall-cmd --reload

# 或使用iptables
iptables -A INPUT -p tcp --dport 5000 -j ACCEPT
service iptables save
```

---

## 第四步：启动应用

### 临时启动（测试用）

```bash
cd /opt/jiandaoyun-daily
source venv/bin/activate
python app.py
```

### 后台运行（推荐）

```bash
cd /opt/jiandaoyun-daily
source venv/bin/activate
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > gunicorn.log 2>&1 &

# 查看日志
tail -f gunicorn.log
```

### 配置开机自启（最推荐）

创建systemd服务：

```bash
cat > /etc/systemd/system/jiandaoyun-daily.service << 'EOF'
[Unit]
Description=Jiandaoyun Daily Report System
After=network.target

[Service]
Type=notify
User=root
WorkingDirectory=/opt/jiandaoyun-daily
Environment="PATH=/opt/jiandaoyun-daily/venv/bin"
ExecStart=/opt/jiandaoyun-daily/venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 app:app --timeout 120
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 启动服务
systemctl daemon-reload
systemctl start jiandaoyun-daily
systemctl enable jiandaoyun-daily

# 查看状态
systemctl status jiandaoyun-daily
```

---

## 第五步：配置微信服务号

### 5.1 配置服务器地址

1. 登录微信公众平台：https://mp.weixin.qq.com
2. 进入：**设置与开发 → 基本配置**
3. 点击"修改配置"，填写：

```
服务器地址(URL)：http://你的域名/wechat
或：http://8.137.123.168:5000/wechat

Token：MyWeChat2026htjiac

EncodingAESKey：点击"随机生成"

消息加解密方式：明文模式
```

4. 点击"提交"（确保应用已启动）

### 5.2 配置IP白名单

1. 在"基本配置"页面找到"IP白名单"
2. 添加：`8.137.123.136`

### 5.3 配置网页授权域名

1. 进入：**设置与开发 → 公众号设置 → 功能设置**
2. 找到"网页授权域名"
3. 填写你的域名（不要加http://）
4. 下载验证文件，上传到服务器

```bash
# 在服务器上
cd /opt/jiandaoyun-daily
# 将验证文件上传到这里
```

### 5.4 配置自定义菜单

1. 进入：**自定义菜单**
2. 添加菜单：

```
菜单名称：身份绑定
菜单类型：跳转网页
页面地址：http://你的域名/bind?openid=OPENID
```

3. 保存并发布

### 5.5 配置模板消息

1. 进入：**功能 → 模板消息**
2. 搜索并添加"工作提醒"类模板
3. 获取模板ID
4. 更新 `config.py` 中的 `WECHAT_TEMPLATE_ID`

---

## 第六步：测试功能

### 测试应用是否运行

```bash
curl http://localhost:5000/
# 应该返回：{"status":"ok","message":"简道云日报提醒系统运行中"}
```

### 测试绑定接口

```bash
curl -X POST http://localhost:5000/api/bind \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "17608248238",
    "openid": "test_openid"
  }'
```

### 测试微信功能

1. 用微信扫码关注你的服务号
2. 点击"身份绑定"菜单
3. 输入手机号
4. 查看是否绑定成功

---

## 常用命令

### 查看日志

```bash
# 查看应用日志
journalctl -u jiandaoyun-daily -f

# 或查看gunicorn日志
tail -f /opt/jiandaoyun-daily/gunicorn.log
```

### 重启应用

```bash
systemctl restart jiandaoyun-daily
```

### 停止应用

```bash
systemctl stop jiandaoyun-daily
```

### 更新代码

```bash
cd /opt/jiandaoyun-daily
git pull
source venv/bin/activate
pip install -r requirements.txt
systemctl restart jiandaoyun-daily
```

---

## 故障排查

### 应用无法启动

```bash
# 查看详细错误
journalctl -u jiandaoyun-daily -n 50

# 检查端口占用
netstat -tlnp | grep 5000
```

### 微信验证失败

1. 确认应用已启动：`systemctl status jiandaoyun-daily`
2. 确认防火墙已开放端口：`firewall-cmd --list-ports`
3. 确认Token配置正确
4. 查看应用日志

### 无法发送消息

1. 检查access_token：访问 `http://localhost:5000/api/test_reminder`
2. 检查模板ID是否正确
3. 检查用户是否已关注服务号

---

## 下一步

- 配置HTTPS（推荐）：参考完整部署指南
- 配置Nginx反向代理：参考完整部署指南
- 配置自动备份：参考完整部署指南

详细内容请查看：`部署指南.md`
