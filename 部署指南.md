# 部署指南

## 一、服务器环境准备

### 1.1 系统要求
- 服务器IP: 8.137.123.138
- Python 3.8+
- pip

### 1.2 安装Python依赖

```bash
# 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

## 二、微信服务号配置

### 2.1 配置服务器地址

1. 登录微信公众平台：https://mp.weixin.qq.com
2. 进入"开发" -> "基本配置"
3. 配置服务器地址（URL）：`http://8.137.123.138:5000/wechat`
4. 配置Token（需要在app.py中设置相同的token）
5. 点击"提交"验证

### 2.2 配置自定义菜单

1. 进入"自定义菜单"
2. 添加菜单按钮"身份绑定"
3. 菜单类型选择"跳转网页"
4. 页面地址填写：`http://8.137.123.138:5000/bind?openid=OPENID`
   - 注意：OPENID是微信的占位符，会自动替换为用户的openid

### 2.3 获取网页授权

如果需要获取用户openid，需要配置网页授权：

1. 进入"接口权限" -> "网页授权"
2. 修改授权回调域名为：`8.137.123.138`

## 三、启动应用

### 3.1 开发环境

```bash
python app.py
```

### 3.2 生产环境（使用gunicorn）

```bash
# 安装gunicorn
pip install gunicorn

# 启动应用（4个worker进程）
gunicorn -w 4 -b 0.0.0.0:5000 app:app

# 后台运行
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > gunicorn.log 2>&1 &
```

### 3.3 使用systemd管理服务

创建服务文件 `/etc/systemd/system/jiandaoyun-daily.service`：

```ini
[Unit]
Description=Jiandaoyun Daily Report System
After=network.target

[Service]
User=root
WorkingDirectory=/path/to/your/project
Environment="PATH=/path/to/your/venv/bin"
ExecStart=/path/to/your/venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl start jiandaoyun-daily
sudo systemctl enable jiandaoyun-daily
sudo systemctl status jiandaoyun-daily
```

## 四、Nginx反向代理（可选）

如果需要使用域名或HTTPS，可以配置Nginx：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## 五、测试功能

### 5.1 测试绑定接口

```bash
curl -X POST http://8.137.123.138:5000/api/bind \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "17608248238",
    "openid": "test_openid_123"
  }'
```

### 5.2 测试流程

1. 用户关注微信服务号
2. 点击"身份绑定"菜单
3. 输入手机号
4. 系统查询简道云员工档案
5. 绑定成功，返回员工姓名

## 六、常见问题

### 6.1 端口被占用

```bash
# 查看端口占用
lsof -i:5000

# 杀死进程
kill -9 <PID>
```

### 6.2 防火墙配置

```bash
# 开放5000端口
sudo firewall-cmd --zone=public --add-port=5000/tcp --permanent
sudo firewall-cmd --reload
```

### 6.3 查看日志

```bash
# 查看应用日志
tail -f gunicorn.log

# 查看systemd服务日志
sudo journalctl -u jiandaoyun-daily -f
```

## 七、安全建议

1. 生产环境修改 `config.py` 中的 `SECRET_KEY`
2. 使用HTTPS协议（配置SSL证书）
3. 限制API访问频率
4. 定期备份数据
5. 不要将敏感信息提交到代码仓库
