# 部署指南

## 📋 部署前准备清单

- ✅ 服务器已准备（IP: 8.137.123.168）
- ✅ 域名ICP备案已通过
- ✅ 微信服务号已认证
- ✅ 简道云API配置已完成

---

## 一、服务器环境准备

### 1.1 连接服务器

```bash
# 使用SSH连接到服务器
ssh root@8.137.123.136
```

### 1.2 安装必要软件

```bash
# 更新系统
yum update -y  # CentOS/RHEL
# 或
apt update && apt upgrade -y  # Ubuntu/Debian

# 安装Python 3.8+
yum install python3 python3-pip -y  # CentOS/RHEL
# 或
apt install python3 python3-pip -y  # Ubuntu/Debian

# 安装Git
yum install git -y  # CentOS/RHEL
# 或
apt install git -y  # Ubuntu/Debian

# 验证安装
python3 --version
pip3 --version
```

### 1.3 创建项目目录

```bash
# 创建项目目录
mkdir -p /opt/jiandaoyun-daily
cd /opt/jiandaoyun-daily
```

---

## 二、部署后端代码

### 2.1 上传代码到服务器

**方法1：使用Git（推荐）**

```bash
# 如果代码在Git仓库中
cd /opt/jiandaoyun-daily
git clone <你的仓库地址> .

# 或者从本地推送
# 在本地项目目录执行：
git init
git add .
git commit -m "Initial commit"
git remote add origin <你的仓库地址>
git push -u origin main
```

**方法2：使用SCP上传**

```bash
# 在本地电脑执行（将整个项目上传到服务器）
scp -r /本地项目路径/* root@8.137.123.168:/opt/jiandaoyun-daily/
```

**方法3：使用FTP工具**
- 使用FileZilla、WinSCP等工具上传

### 2.2 安装Python依赖

```bash
cd /opt/jiandaoyun-daily

# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级pip
pip install --upgrade pip

# 安装项目依赖
pip install -r requirements.txt

# 安装生产环境需要的额外包
pip install gunicorn
```

### 2.3 配置环境变量

```bash
# 编辑config.py，确保配置正确
vim config.py

# 或者创建.env文件（如果使用环境变量）
cat > .env << EOF
JIANDAOYUN_API_KEY=你的简道云API_KEY
JIANDAOYUN_APP_ID=你的APP_ID
WECHAT_APPID=你的微信APPID
WECHAT_APPSECRET=你的微信APPSECRET
EOF
```

### 2.4 测试应用

```bash
# 激活虚拟环境
source venv/bin/activate

# 运行测试脚本
python test_api.py

# 测试Flask应用
python app.py
# 在另一个终端测试：curl http://localhost:5000/
```

---

## 三、配置微信服务号

### 3.1 配置服务器地址（重要！）

1. **登录微信公众平台**
   - 访问：https://mp.weixin.qq.com
   - 使用管理员账号登录

2. **配置服务器**
   - 进入：设置与开发 → 基本配置
   - 点击"修改配置"
   
3. **填写服务器配置**
   ```
   服务器地址(URL)：http://你的域名/wechat
   或：http://8.137.123.168:5000/wechat
   
   Token：MyWeChat2026htjiac
   （这个要和app.py中的mytoken保持一致）
   
   EncodingAESKey：点击"随机生成"
   
   消息加解密方式：明文模式（测试）或安全模式（生产）
   ```

4. **启动应用并提交验证**
   ```bash
   # 在服务器上启动应用
   cd /opt/jiandaoyun-daily
   source venv/bin/activate
   python app.py
   ```
   
5. **点击"提交"按钮**
   - 微信会向你的服务器发送验证请求
   - 如果配置正确，会显示"提交成功"

### 3.2 配置IP白名单

1. 进入：设置与开发 → 基本配置
2. 找到"IP白名单"
3. 添加你的服务器IP：`8.137.123.168`

### 3.3 配置网页授权域名

1. 进入：设置与开发 → 公众号设置 → 功能设置
2. 找到"网页授权域名"
3. 点击"设置"
4. 填写域名：`你的域名`（不要加http://）
5. 下载验证文件，上传到服务器根目录

```bash
# 在服务器上创建验证文件
cd /opt/jiandaoyun-daily
# 将下载的验证文件上传到这里
```

### 3.4 配置自定义菜单

1. **进入菜单管理**
   - 设置与开发 → 自定义菜单

2. **创建菜单**
   ```
   菜单名称：身份绑定
   菜单类型：跳转网页
   页面地址：http://你的域名/bind?openid=OPENID
   ```
   
   注意：`OPENID` 是微信的占位符，会自动替换

3. **保存并发布**

### 3.5 申请模板消息

1. 进入：功能 → 模板消息
2. 搜索"日报提醒"相关模板
3. 添加模板，获取模板ID
4. 将模板ID配置到 `config.py` 中的 `WECHAT_TEMPLATE_ID`

---

## 四、生产环境部署

### 4.1 配置防火墙

```bash
# CentOS/RHEL
firewall-cmd --zone=public --add-port=5000/tcp --permanent
firewall-cmd --reload

# Ubuntu/Debian (使用ufw)
ufw allow 5000/tcp
ufw reload

# 或者使用iptables
iptables -A INPUT -p tcp --dport 5000 -j ACCEPT
service iptables save
```

### 4.2 使用Gunicorn启动（推荐）

```bash
cd /opt/jiandaoyun-daily
source venv/bin/activate

# 前台运行（测试用）
gunicorn -w 4 -b 0.0.0.0:5000 app:app

# 后台运行
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > gunicorn.log 2>&1 &

# 查看进程
ps aux | grep gunicorn

# 查看日志
tail -f gunicorn.log
```

### 4.3 配置Systemd服务（推荐）

创建服务文件：

```bash
vim /etc/systemd/system/jiandaoyun-daily.service
```

写入以下内容：

```ini
[Unit]
Description=Jiandaoyun Daily Report System
After=network.target

[Service]
Type=notify
User=root
WorkingDirectory=/opt/jiandaoyun-daily
Environment="PATH=/opt/jiandaoyun-daily/venv/bin"
ExecStart=/opt/jiandaoyun-daily/venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 app:app --timeout 120
ExecReload=/bin/kill -s HUP $MAINPID
KillMode=mixed
TimeoutStopSec=5
PrivateTmp=true
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
# 重载systemd配置
systemctl daemon-reload

# 启动服务
systemctl start jiandaoyun-daily

# 设置开机自启
systemctl enable jiandaoyun-daily

# 查看状态
systemctl status jiandaoyun-daily

# 查看日志
journalctl -u jiandaoyun-daily -f

# 重启服务
systemctl restart jiandaoyun-daily

# 停止服务
systemctl stop jiandaoyun-daily
```

### 4.4 配置Nginx反向代理（可选，推荐）

安装Nginx：

```bash
# CentOS/RHEL
yum install nginx -y

# Ubuntu/Debian
apt install nginx -y
```

创建配置文件：

```bash
vim /etc/nginx/conf.d/jiandaoyun-daily.conf
```

写入以下内容：

```nginx
server {
    listen 80;
    server_name 你的域名;  # 例如：daily.example.com

    # 日志
    access_log /var/log/nginx/jiandaoyun-daily-access.log;
    error_log /var/log/nginx/jiandaoyun-daily-error.log;

    # 反向代理到Flask应用
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 静态文件（如果有）
    location /static {
        alias /opt/jiandaoyun-daily/static;
        expires 30d;
    }
}
```

启动Nginx：

```bash
# 测试配置
nginx -t

# 启动Nginx
systemctl start nginx
systemctl enable nginx

# 重载配置
systemctl reload nginx
```

### 4.5 配置HTTPS（强烈推荐）

使用Let's Encrypt免费SSL证书：

```bash
# 安装certbot
yum install certbot python3-certbot-nginx -y  # CentOS
# 或
apt install certbot python3-certbot-nginx -y  # Ubuntu

# 自动配置SSL
certbot --nginx -d 你的域名

# 测试自动续期
certbot renew --dry-run
```

---

## 五、测试部署

### 5.1 测试基本功能

```bash
# 测试首页
curl http://你的域名/
curl http://8.137.123.168:5000/

# 测试微信接口（需要微信验证）
curl http://你的域名/wechat

# 测试绑定接口
curl -X POST http://你的域名/api/bind \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "17608248238",
    "openid": "test_openid_123"
  }'
```

### 5.2 测试微信功能

1. **关注你的微信服务号**
2. **点击"身份绑定"菜单**
3. **输入手机号进行绑定**
4. **查看是否绑定成功**

### 5.3 测试定时任务

```bash
# 手动触发一次日报检查
curl -X POST http://你的域名/api/test_reminder

# 查看日志
tail -f /var/log/nginx/jiandaoyun-daily-access.log
journalctl -u jiandaoyun-daily -f
```

---

## 六、监控和维护

### 6.1 查看应用日志

```bash
# 查看systemd服务日志
journalctl -u jiandaoyun-daily -f

# 查看Nginx日志
tail -f /var/log/nginx/jiandaoyun-daily-access.log
tail -f /var/log/nginx/jiandaoyun-daily-error.log

# 查看Gunicorn日志（如果使用nohup）
tail -f /opt/jiandaoyun-daily/gunicorn.log
```

### 6.2 重启应用

```bash
# 重启systemd服务
systemctl restart jiandaoyun-daily

# 或者重启Gunicorn进程
pkill gunicorn
cd /opt/jiandaoyun-daily
source venv/bin/activate
nohup gunicorn -w 4 -b 0.0.0.0:5000 app:app > gunicorn.log 2>&1 &
```

### 6.3 更新代码

```bash
cd /opt/jiandaoyun-daily

# 拉取最新代码
git pull

# 激活虚拟环境
source venv/bin/activate

# 更新依赖（如果有变化）
pip install -r requirements.txt

# 重启服务
systemctl restart jiandaoyun-daily
```

### 6.4 备份数据

```bash
# 创建备份脚本
vim /opt/backup.sh
```

写入：

```bash
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# 备份项目文件
tar -czf $BACKUP_DIR/jiandaoyun-daily-$DATE.tar.gz /opt/jiandaoyun-daily

# 删除30天前的备份
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "Backup completed: $DATE"
```

设置定时备份：

```bash
chmod +x /opt/backup.sh

# 添加到crontab（每天凌晨2点备份）
crontab -e
# 添加：0 2 * * * /opt/backup.sh
```

---

## 七、常见问题排查

### 7.1 应用无法启动

```bash
# 查看详细错误
journalctl -u jiandaoyun-daily -n 50

# 检查端口占用
netstat -tlnp | grep 5000
lsof -i:5000

# 检查Python环境
source /opt/jiandaoyun-daily/venv/bin/activate
python -c "import flask; print(flask.__version__)"
```

### 7.2 微信验证失败

1. 检查服务器是否正常运行
2. 检查防火墙是否开放端口
3. 检查Token是否一致
4. 查看应用日志

### 7.3 无法发送消息

1. 检查access_token是否获取成功
2. 检查模板ID是否正确
3. 检查用户是否已关注服务号
4. 查看微信公众平台的错误日志

### 7.4 定时任务不执行

```bash
# 查看定时任务日志
journalctl -u jiandaoyun-daily | grep "scheduler"

# 手动触发测试
curl -X POST http://localhost:5000/api/test_reminder
```

---

## 八、安全建议

### 8.1 修改默认配置

```python
# 在config.py中修改
SECRET_KEY = '生成一个随机的密钥'  # 使用：python -c "import os; print(os.urandom(24).hex())"
```

### 8.2 限制访问

```nginx
# 在Nginx配置中添加IP限制（可选）
location /api/test_reminder {
    allow 127.0.0.1;
    deny all;
    proxy_pass http://127.0.0.1:5000;
}
```

### 8.3 定期更新

```bash
# 定期更新系统和依赖
yum update -y
pip install --upgrade -r requirements.txt
```

---

## 九、快速部署脚本

创建一键部署脚本：

```bash
vim /opt/deploy.sh
```

写入：

```bash
#!/bin/bash

echo "开始部署简道云日报系统..."

# 进入项目目录
cd /opt/jiandaoyun-daily

# 拉取最新代码
git pull

# 激活虚拟环境
source venv/bin/activate

# 安装/更新依赖
pip install -r requirements.txt

# 重启服务
systemctl restart jiandaoyun-daily

# 等待服务启动
sleep 3

# 检查服务状态
systemctl status jiandaoyun-daily

echo "部署完成！"
```

使用：

```bash
chmod +x /opt/deploy.sh
/opt/deploy.sh
```

---

## 十、部署检查清单

部署完成后，请检查以下项目：

- [ ] 服务器可以正常访问
- [ ] Python应用正常运行
- [ ] 微信服务器配置验证通过
- [ ] 自定义菜单配置完成
- [ ] 网页授权域名配置完成
- [ ] 模板消息配置完成
- [ ] 测试绑定功能正常
- [ ] 测试消息发送正常
- [ ] 定时任务正常执行
- [ ] 日志记录正常
- [ ] 配置了HTTPS（推荐）
- [ ] 配置了自动备份
- [ ] 配置了开机自启

---

## 需要帮助？

如果遇到问题，请检查：
1. 应用日志：`journalctl -u jiandaoyun-daily -f`
2. Nginx日志：`tail -f /var/log/nginx/jiandaoyun-daily-error.log`
3. 微信公众平台的开发者工具
